import Grid from "../components/Grid";
import styles from "./styles.module.css";

const Departures = () => {
  return <div className={styles.departure}>
      <header>Departures</header>
      <Grid />
    </div>
};

export default Departures;
